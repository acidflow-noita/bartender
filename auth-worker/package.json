{
  "name": "bartender-auth-worker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env local --port 8787",
    "dev:remote": "wrangler dev --env local --port 8787 --remote",
    "deploy": "wrangler deploy",
    "deploy:test": "wrangler deploy --env test"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "wrangler": "^4.25.1"
  }
}
